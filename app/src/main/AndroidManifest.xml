<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================================== -->
    <!-- PERMISOS REQUERIDOS PARA LA APP            -->
    <!-- ========================================== -->
    <!-- Permiso para sincronizar datos con la API RESTful -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permiso para usar la cámara (Escaneo de QR de contenedores/clientes) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Permisos para guardar reportes en PDF/Excel (Hasta API 32) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoTrack2"
        tools:targetApi="31">

        <!-- ========================================== -->
        <!-- DECLARACIÓN DE ACTIVITIES                  -->
        <!-- ========================================== -->

        <!-- 1. Actividad Principal (Pantalla de Inicio) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Actividad para Registrar Residuos -->
        <activity
            android:name=".RegistroActivity"
            android:exported="false" />

        <!-- 3. Actividad para Ver Reportes y Filtros -->
        <activity
            android:name=".ReportesActivity"
            android:exported="false" />

        <activity
            android:name=".EstadisticasActivity"
            android:exported="false" />

        <activity
            android:name=".EditarActivity"
            android:exported="false" />

    </application>

</manifest>